<% if current_owner %>
  <% if current_owner.inn %>
    <li class="nav-item"><%= link_to "Minha Pousada", inn_path(current_owner.inn), class: "nav-link" %></li>
  <% else %>
    <li class="nav-item me-2"><%= link_to "Cadastrar pousada", new_inn_path, class: "btn btn-primary" %></li>
  <% end %>
    <li class="nav-item me-2"><%= button_to "Sair", destroy_owner_session_path, method: :delete, class:"btn btn-danger" %></li>
<% else %>
  <span class="navbar-text">
    <%= current_user.name %>
  </span>
  <li class="nav-item me-2"><%= link_to "Minhas reservas", my_reservations_path, class: "nav-link" %></li>
  <li class="nav-item me-2"><%= button_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-danger" %></li>
<% end %>