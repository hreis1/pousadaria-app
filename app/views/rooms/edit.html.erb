<section id="edit-room">
  <h2>Editar quarto</h2>

  <% if @room.errors.any? %>
    <div>
      <% @room.errors.full_messages.each do |message| %>
        <p><%= message %></p>
      <% end %>
    </div>  
  <% end %>

  <%= render 'form' %>
</section>
